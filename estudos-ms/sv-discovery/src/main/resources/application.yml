server:
  port: 8081

spring:
  application:
    name: sv-discovery

eureka: ##configuração do service discovery eureka
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:${server.port}/eureka/
